<main class="content">
    <section id="posts">
        <div class="container">
            <div class="row">
                {{#each cards}}
                    {{> card}}
                {{/each}}
            </div>
        </div>
    </section>

    {{> addCard }}

</main>
